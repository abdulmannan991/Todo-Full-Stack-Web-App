"""
Midnight Genesis Backend Package

This package contains the FastAPI backend for the Midnight Genesis todo application.

To run the server from the project root:
    uvicorn backend.main:app --reload --port 8000

CRITICAL: Always run uvicorn from the project root directory, NOT from inside backend/.
"""
